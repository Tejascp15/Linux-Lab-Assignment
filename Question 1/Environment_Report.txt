Question 1: Linux Environment Verification

Command: whoami

Output:
coder

Observation:
The whoami command displays the username of the currently logged-in user. The current user is coder.


Command: id

Output:
uid=1000(coder) gid=1000(coder) groups=1000(coder),27(sudo)

Observation:
The id command displays the user ID, group ID, and the groups associated with the current user.


Command: echo $SHELL

Output:
/bin/sh

Observation:
This command displays the current shell being used by the terminal.


Command: pwd

Output:
/home/coder

Observation:
The pwd command prints the current working directory.



Command: ls -la

Observation:
The ls -la command lists all files and directories, including hidden files, along with permissions and ownership information.


Command: ping -c 4 google.com

Output:
Destination Port Unreachable
100% packet loss

Observation:
Ping is blocked in the Coursera Lab environment.


Command:
curl -I https://google.com

Output:
HTTP/1.1 200 OK

Observation:
The curl command successfully connected to Google's server, confirming that internet connectivity is available.